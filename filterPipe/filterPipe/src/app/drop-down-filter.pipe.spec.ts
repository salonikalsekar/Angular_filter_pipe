import { DropDownFilterPipe } from './drop-down-filter.pipe';

describe('DropDownFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DropDownFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
